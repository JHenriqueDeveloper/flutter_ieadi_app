import 'package:flutter/material.dart';

import '../../../widgets/widgets.dart';

class PastoresScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return DefaultScreen(
      title: 'pastores',
      backToPage: 8,
      //padding: const EdgeInsets.only(bottom: 32),
      children: [],
    );
  }
}
